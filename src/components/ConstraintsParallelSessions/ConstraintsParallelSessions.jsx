import React from 'react';
import ContentHeader from '../ContentHeader/ContentHeader';

function ConstraintsParallelSessions() {
    return (
        <div>
            <ContentHeader header={'Parallel Sessions'} />
        </div>
    );
}

export default ConstraintsParallelSessions;
